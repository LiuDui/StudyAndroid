<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.uicontrollers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainLayoutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LinearLayoutShowActivity" />
        <activity android:name=".LinearLayoutShowActivity2" />
        <activity android:name=".RelativeLayoutShowActivity" />
        <activity android:name=".FrameLayoutShowActivity" />
        <activity android:name=".PercentageLayoutShowActivity" />
        <activity android:name=".LayoutWeightShowActivity" />
        <activity android:name=".LayoutWeightShowActivity2"></activity>
    </application>

</manifest>